'use babel';

import DeleteWhitelinesView from '../lib/delete-whitelines-view';

describe('DeleteWhitelinesView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
